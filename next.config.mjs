/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow static bookai.html dashboard to be served from public/
  // without interfering with App Router routes.
};

export default nextConfig;
